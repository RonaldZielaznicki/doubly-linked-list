/**
 * @template T
 * @param {Array<T>} values
 * @returns {DoublyLinkedList<T>}
 */
export function of<T>(...values: T[]): DoublyLinkedList<T>;
import { DoublyLinkedList } from '../doubly-linked-list.js';
