/**
 * @param {DoublyLinkedList<T>} list
 * @param {number} index
 * @param {any} value
 * @template T
 */
export function setAt<T>(list: DoublyLinkedList<T>, index: number, value: any): void;
import { DoublyLinkedList } from '../doubly-linked-list.js';
